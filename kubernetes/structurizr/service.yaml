apiVersion: v1
kind: Service
metadata:
  name: structurizr-lite
  namespace: structurizr
spec:
  type: ClusterIP
  selector:
    app: structurizr-lite
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
